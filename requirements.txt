# ── Pure-Python / pip-only packages ──
# NOTE: gdal, rasterio, geopandas, shapely, stackstac, leafmap,
#       localtileserver, rioxarray, numpy, xarray, pandas, matplotlib,
#       scipy, openpyxl are ALL installed via conda-forge in the Dockerfile.
#       Do NOT add them back here — pip would overwrite the conda-linked
#       binaries and break GDAL/SQLite symbol resolution.

torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0
solara==1.57.3
solara-enterprise==1.57.3
solara-server==1.57.3
solara-ui==1.57.3
Authlib==1.6.9
Flask==3.1.3
folium==0.20.0
pystac-client
planetary-computer
omnicloudmask
sentinelhub
s2cloudless
lonboard
starlette==0.36.3
